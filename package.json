{
  "name": "worker-message",
  "version": "0.0.1",
  "description": "Tool for posting/getting messages to/from webworkers with types",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "prepare": "babel ./src/*.js --retain-lines -d ./dist"
  },
  "keywords": [
    "webworkers"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0"
  }
}
